const som = 2 + 4; /* soma */
const diff = 10 - 5; /* subtracao */
const multiply = 20 * 2356; /* multiplicacao */
const division = 40 / 5; /* divisao */
const rest = 24 % 5; /* resto/porcentagem */
const power = Math.pow(5, 2); /* potencia */
const squareRoot = Math.sqrt(2); /* raiz quadrada */

console.log(squareRoot);